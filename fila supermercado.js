let [func, clientes] = prompt("n° funcionários e clientes: ")
  .split(" ")
  .map(Number);
let [vel] = prompt("vel. dos funcionarios: ").split(" ").map(Number);
let [itens] = prompt("itens: ").split(" ").map(Number);
