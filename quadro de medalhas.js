let casos = parseInt(prompt("numero de casos: "));

for (let i = 0; i < casos; i++) {
  let pont = prompt("pais e pontuação: ");

  let ouro = pont[pont.length - 5];
  let prata = pont[pont.length - 3];
  let bronze = pont[pont.length - 1];
}
